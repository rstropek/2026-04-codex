# Copy to .env.local and adjust to your machine.
#
# Absolute path to the SQLite database the web app should read/write.
# Mirror this to whatever path the console CLI uses (by default the file
# `questionnaire.db` in the workspace root).
DATABASE_URL=/absolute/path/to/2026-04-codex/questionnaire.db
